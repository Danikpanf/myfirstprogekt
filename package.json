{
  "name": "game-marketplace",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/web\"",
    "build": "npm run build --prefix apps/api && npm run build --prefix apps/web",
    "db:migrate": "npm run prisma:migrate --prefix apps/api",
    "db:seed": "npm run prisma:seed --prefix apps/api",
    "db:studio": "npm run prisma:studio --prefix apps/api",
    "db:push": "npm run prisma:push --prefix apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
